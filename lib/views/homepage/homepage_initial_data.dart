import 'package:flutter/material.dart';

class HomePageInitialData extends StatelessWidget {
  const HomePageInitialData({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 300,
      width: 380,
      child: Card(
        color: Colors.grey.shade800,
      ),
    );
  }
}
